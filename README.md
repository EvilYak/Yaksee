# Ce dépôt contient deux projets sans rapport

- **[`game/`](./game) — NO CLIP, jeu d'exploration backrooms en 3D.**
  Voir [`game/README.md`](./game/README.md) pour tout (lancement, contrôles,
  build, APK Android, démo en ligne). C'est le projet actif.

- **`Yaksee.py`** — un vieux script CLI de génération de payloads Metasploit,
  conservé pour mémoire mais sans rapport avec le jeu.

Pour travailler sur le jeu : `cd game && npm install && npm run dev -- --host`.
